<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="org.sensorsystem.dao.sensordao.sensordao">

    <insert id="insertsensor" useGeneratedKeys="true" keyProperty="sid" parameterType="sensors">

        insert into
        sensors (sname, nickname, manufacturer,area,partnumber) values(#{sname}, #{nickname}, #{manufacturer},#{area},#{partnumber})

    </insert>


</mapper>