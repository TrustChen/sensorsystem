<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="org.sensorsystem.dao.sensordao.sensordatadao">


    <select id="querySensorData" resultType="sensordata">
        SELECT * from sensordata sd
        WHERE sd.id IN
      (SELECT sd1.id from sensordata sd1, sensormatch m
      WHERE sd1.id=m.sid and m.uid = #{id} )
    </select>


</mapper>